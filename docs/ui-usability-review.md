# UI・操作性の改修記録

## 対象

2026年9月8〜9日の改修。番組表、見逃し検索、ライブ、ライブラリ、共通プレーヤーを実装コードから確認し、並行した担当作業を段階的に統合しています。PRを作らず、検証した単位で `main` に反映しています。

## 主な改善

| 場所 | 修正内容 |
| --- | --- |
| タブとミニプレーヤー | 4タブに整理。古い診断タブの選択状態をライブラリへ移行。ミニプレーヤーの「開く」「再生／一時停止」「停止」を分離。 |
| 見逃し | 検索を常設し、送信時は即時検索。重複エピソードを整理し、配信期限・取得状態・並び順を分かりやすく表示。 |
| 番組表 | 前日／日付／翌日／現在の導線を整理。午前5時を境にした放送日と現在位置の移動、表示局の選択、グリッド範囲制御を修正。文字を大きくすると一覧へ切り替え。 |
| ライブラリ | 空でも全カテゴリを表示し、何もない理由と次の操作を案内。閲覧と複数選択を分離し、カテゴリ変更・転送完了後の古い選択で別のデータを削除しない。 |
| ダウンロード | 拒否理由を操作した画面で表示。Wi-Fi専用／通信条件不明の既存タスクを、単なる「再開」の同意で削除・再作成しない。途中データを維持してWi-Fiを待つか、明示的に最初からの取得を選べる。 |
| 再生 | 小さな画面でも操作を押しやすくし、ネイティブの背景タップ領域から実際のボタン・シークバーの範囲を除外。失敗時の説明・再試行・公式ページへの導線を用意。 |
| 表示 | 意味ベースのフォントとアクセシビリティ時の縦配置を採用。ライブ説明の文字列補間・改行表示も修正。 |

## 再生と詳細画面の追加修正

- 詳細画面を開いたままでも、「配信なし」のキャッシュ期限と新しい配信状態を反映します。表示の時計は30秒ごと、操作時の日時・再生対象は実行時に再評価します。
- 番組表詳細の外側の失敗案内、内側プレーヤーの再試行、失敗詳細シート、中断からの再開は、同じ操作所有者が実行時の時計と再生対象を確認します。番組終了後はその放送回の見逃し検索へ進み、別の局・既知の別放送回に変わった後の古い回復操作は実行しません。
- 再生項目を保持した音声セッションの失敗では、最初から解決し直す代わりに既存項目を再開します。
- まだ読み込み中に一時停止してからライブ画面へ戻っても、処理を重複開始したり、勝手に再生を再開したりしません。
- PiPは画面を離れても、実際の開始・停止処理に必要な間だけ保持します。全画面と通常画面のどちらかが閉じても、もう一方の復帰処理を消しません。アプリ側の停止・再生終了では残ったPiPも停止します。
- 背景の単発タップはUIKitがダブルタップを除外してからコントロールを切り替えます。以前の「先に切り替えて後から打ち消す」方式をやめました。この変更は余白への背景タップが対象です。実際の連続タッチの検証範囲は下記の制限を参照してください。

## 大きな文字と操作の継続性

- ライブラリの大きな文字では、分類名を全幅にし、件数と開閉マークを次の行へ配置します。選択中分類の重複見出しも省きます。件数はライト／ダーク双方に追従する不透明な文字色です。
- 番組行の移動先とダウンロード操作は兄弟要素のまま、アクセシビリティ文字サイズでは縦に並びます。サイズ切り替えで操作側の確認状態を作り直さないレイアウトを使います。
- 番組表一覧は各行に放送局名を折り返して表示します。現在位置へ移動して節見出しが画面外になっても、その行の放送局を識別できます。
- グリッドの現在時刻の線は局間の余白だけに描き、半透明の番組カード越しに文字・バッジを横切らないようにしました。
- プレーヤー背景への実タッチ受信時に、表示中コントロールの古い自動非表示期限を取り消します。非表示のコントロールをタッチ受信だけで表示したり、一時停止・操作保持中の非表示抑制を解除したりしません。
- 縦の短い全画面では時間表示の固有サイズを確保します。大きな文字の場合に加え、通常文字でも利用可能な高さが非常に短い横長の全画面では主要操作を上段へまとめます。番組名は設定メニュー、詳しい失敗理由は既存の詳細画面で確認できます。時間の文字サイズ・44ptの操作領域は縮小していません。
- 横長で短い全画面では、シークバーと時間表示を同じ段に配置して高さを節約します。経過時間と残り時間の間には最低8ptの隙間を確保し、数字がつながって見えないようにします。時間の文字サイズ、読み上げ内容、44ptの操作領域は維持します。
- 全画面の映像は端まで広げたまま、その画面が持つセーフエリアを操作部分へ渡します。閉じる操作・シークバー・時間表示が画面端の安全領域へ入り込む条件を修正しました。
- 描画fixtureは正式なホスティングコントローラーの包含と画面へのサイズ提案を使用します。単独のプレーヤーキャンバスと通常画面のセーフエリア扱いを区別します。これは実アプリの全ルート操作の検証ではありません。

## ダウンロードの通信条件

Appleの既存ダウンロードタスクは、作成時の通信設定を後から変更できません。現在のアプリ設定だけを変更しても、既存のWi-Fi専用タスクがモバイル通信可能になるわけではありません。

- 「再開」は途中データを残す操作です。元タスクが失われていても、無断で最初から取り直しません。
- 「今回だけモバイル通信で最初からやり直す」は、途中データの削除を説明した上で実行します。事前条件の確認で失敗した場合は、途中データを維持します。
- 一度の同意でWi-Fi限定設定を恒久的に解除しません。
- 個別の再取得に加え、復旧のお知らせからの一括やり直しも対象・件数・途中データの削除を確認します。同意は一度限りで、実行時に状態が一致する停止・失敗中の対象だけへ適用します。確認中に待機中・転送中・保存済みへ変わった対象や、別の停止状態になった対象は残します。古いお知らせの対象も更新します。
- 再起動前のタスクの通信条件が不明な場合は、安全側に扱います。Wi-Fi接続後の再開、または明示的な最初からの取得が必要になることがあります。

## ダウンロード復元中の操作

- 非同期の準備中に中止・置き換えたタスクを、後から返った準備結果で再開しません。古いタスクの遅延通知を、新しいダウンロードの進捗・保存先と混同しないようにします。
- 復元したタスクはセッションとタスクの両方で識別します。所有者が決まる前に届いた完了・失敗・保存先の通知も、その復元対象と世代が一致する場合だけ反映します。
- 復元一覧を取得する途中で見つかったタスクにも中止・削除を届けます。残りの一覧取得を待ち続けたり、空の保存一覧への復元し直しで中止要求を失ったりしません。重複した制御要求を抑え、古い復元処理の終了時に、新しく採用された同じタスクを停止しません。
- 復元候補を保持しているだけでは再開やモバイル通信を許可しません。途中データを残す再開と、明示的な最初からの取得の区別は維持します。

## 確認済みの検証

| 対象 | 結果 |
| --- | --- |
| 元の `6416ed4` | Simulatorビルド成功。421件のテスト中1件スキップ・1件失敗（既存の全画面タップ判定テスト）。 |
| `0442b44` | 509件中1件スキップ・失敗0。既存のタップ判定アサーションを保持し、実際に有効な再生項目を使うfixtureへ強化。 |
| `036fb01` の描画fixture | 1件のopt-in XCTestで19通りのPNGを生成。ライト／ダーク、320pt幅、最大アクセシビリティ文字サイズ、640×240ptの全画面を含む。 |
| `95a5494` | ダウンロードの実ストア＋スタブdriver回帰を含む524件中2件スキップ・失敗0。 |
| `0bd55a3` | 番組表詳細8件、再生・PiP・ジェスチャ13件を追加。描画fixtureを有効化した全545件中1件スキップ・失敗0。19通りの画面も再生成。 |
| `0f87b54` | ライブラリ配置6件、番組表6件、タッチ境界3件の回帰を追加。描画fixture有効で全560件中1件スキップ・失敗0。19通りの画面を再生成。 |
| `18c9fdd` | お知らせ復旧9件と、実際の内側プレーヤー／失敗シートのcallback経路など14件を追加。全583件中1件スキップ・失敗0。19通りの画面も再生成。 |
| `3d0480b` | 時間表示修正だけを独立統合し、全583件中1件スキップ・失敗0。19画像を再生成。描画回帰で時間の実寸境界、主要操作との非重複、閉じる操作の44ptを検査。 |
| `9cfcd22` | 実FullScreenPlaybackViewを含む5テスト・10配置条件を追加。非ゼロのセーフエリアで失敗した条件を、判定を変えずに修正。全588件中1件スキップ・失敗0、19画像を再生成。 |
| `8e9cc43` | 画面修正だけの独立公開版。全589件中1件スキップ・失敗0、19画像を再生成。専用6テストには、同一画面で安全領域を2回変更する4条件を含み、映像レイヤー・再生項目・一時停止位置の継続も検査。 |
| `8f46685` | 復元処理45件と画面配置7件を含む119件が失敗0。描画fixture有効の全635件中1件スキップ・失敗0、19画像を生成。ただし画像確認で時間文字の隣接を見つけ、間隔の判定を追加。 |
| `3363c39` | 時間表示の最低8ptの間隔を追加した最終コード。強化した同じ7テストを含む全635件中1件スキップ・失敗0、19画像を再生成。文字サイズ・操作領域・境界判定を緩めず、より狭い安全領域と再生状態の継続も検査。 |

前公開版 `a47c394` のCIでは、利用可能な高さが153ptになる通常文字の全画面で、時間表示が安全領域を3pt越える2つの境界違反を検出しました。今回の高さに応じた配置変更は、ローカルでも再現した同種の不足を、文字サイズや44pt判定を変えずに修正します。旧CIの失敗と、新版のCI結果は区別します。

公開版 `c88c075` のCI（Xcode 16.4 / iOS 18.5 / iPhone 16 Pro）では、最大文字・安全領域147ptの条件で時間表示に4件の境界違反が残りました。同じメソッド内の4件が3回の試行で再現しています。横並びの修正後は、ローカルで安全領域137→133ptの条件も検査しました。旧版のCI失敗と新版のCI結果は区別し、実行結果はGitHub Actionsを参照してください。

復元待機中の中止・削除の負例は、旧Driverに同じ45テストを適用して4メソッド内10件のアサーション失敗を確認し、修正後は45件が成功しました。時間文字の隣接も、間隔判定を追加した修正前の7テストで5メソッド内22件の失敗を確認しています。期待失敗化や判定の引き下げで通したものではありません。ダウンロードの試験は実ストア・実Driverと制御可能なタスク境界を使いますが、実HLS転送や通信の停止遅延・料金を測ったものではありません。

実行環境はXcode 26.6 / iOS 26.5 Simulator。通常のテストでスキップする2件は、実TVerライブのスモークテストと、明示的に有効化する画面描画fixtureです。19通りの画面は19個の独立したXCTestではなく、1個の描画テスト内のケースです。

画像による限定確認では、`18c9fdd` の番組表グリッド、最大文字の番組表一覧・ライブラリの3画面と、同じ時間表示修正を含む `9faad99` の640×240pt全画面1画面を確認しました。全画面の時間文字の下端欠けは解消しています。本版では `3363c39` の640×240pt・最大文字の全画面画像も確認し、経過時間と残り時間の隙間、両方の文字全体の表示を確かめました。確認には拡縮しない非可逆圧縮プレビューを使っており、同一版の全19画像を目視確認したという意味ではありません。

## 長時間表示の追加回帰（2026年9月9日）

`d25eca2` の表示コードを変えず、実際の `FullScreenPlaybackView` に3時間のローカルWAVを読み込み、一時停止したまま90分の位置へシークする回帰を追加しました。元の7メソッドと全ての既存アサーション、通常fixtureの120秒／75秒の設定は維持しています。

- 最大アクセシビリティ文字サイズ、高さ240pt、幅528／560／640ptで `1:30:00` と `-1:30:00` を検査します。WAVは各172,800,044bytesです。
- 表示用の時刻だけでなく、実AVPlayerの5400秒へのシーク完了と、AVPlayerItemの10800秒の長さを照合します。
- 時計の固有サイズ、最低8ptの間隔、測定対象の44pt操作領域、実画面とnative safe-area内への収まりを検査します。測定対象は再生／シークバー／上部の閉じる操作で、補助ボタン全ての当たり判定を保証するものではありません。
- この3条件では幅不足を再現しませんでした。フォント縮小、下部の全画面解除の削除、共有close callbackの変更は行っていません。

| 幅×高さ（pt） | シークバーの実測幅（pt） | 時計間の実測間隔（pt） |
| --- | --- | --- |
| 528×240 | 44 | 8 |
| 560×240 | 約75.33 | 8 |
| 640×240 | 約155.33 | 8 |

診断初版の上記3画像を、原寸の8-bit閲覧コピーで全て目視確認しました。経過／残り時間の全文と分離、表示された操作アイコンに目視上の欠け・重なりがないことを確認しています。これは別ホストで生成した既存19画像の流用ではありません。画像確認後に追加したnative時刻・safe-area・描画成功のアサーションも含め、最終版はXcode 26.6／iOS 26.5／iPhone 17e Simulatorで全636件中1件スキップ・失敗0でした。最終版は1回の全体実行で通過し、自動再試行・Simulatorの消去は行っていません。

最終実行では従来の画面19枚と長時間時計の3枚を生成しました。生成した全22画像を目視確認したという意味ではありません。PNGの描画成功と寸法も検査し、任意の画像取得・保存はレイアウトとnative identityの検査後に実行します。画像保存の失敗で、そのケースのレイアウト検査を飛ばさない構成です。

この結果は上記の固定3条件に限ります。任意の動画長・左右セーフエリア・文字サイズの全組合せ、実際のタッチ、VoiceOver、指を置いたままのシーク継続、PiP対応時の配置は未検証です。probeの出現待ちと短いlayout反映待ちは、全OSでの安定配置や実操作の証明ではありません。

## 画面を再生成する

利用可能なSimulatorを `xcrun simctl list devices available` で確認し、対象のUDIDを指定します。

```bash
xcodegen generate
TEST_RUNNER_RECORD_UI_SNAPSHOTS=1 xcodebuild \
  -project TVerClient.xcodeproj \
  -scheme TVerClient \
  -destination "platform=iOS Simulator,id=${SIMULATOR_UDID}" \
  -parallel-testing-enabled NO \
  -only-testing:TVerClientTests/UIRenderingRegressionTests \
  -resultBundlePath /tmp/TVerClient-UIReview.xcresult \
  CODE_SIGNING_ALLOWED=NO test
```

結果はXCTestの添付画像と、アプリコンテナ内の `Documents/UIReviewSnapshots` に保存されます。再実行時は新しいresult bundleのパスを使ってください。

## 継続調査項目

このチェックポイントでは、次の範囲を未検証のまま残しています。実端末で不具合を再現したという意味ではありません。

- 実際のバックグラウンドHLSタスクでの復元・停止遅延・保存データの変化、列挙が返らない場合の解放期限、複数世代の組合せは追加検証が必要です。
- 長時間の時刻文字列は上記の固定3条件を追加検証しました。指を置いたままのシーク中に配置を切り替える条件、VoiceOverの実フォーカス順は未検証です。
- 別々のPiPコーディネーターを伴う画面再入場時のレイヤー継続性は検証候補です。実端末の映像停止・黒画面を再現したという意味ではありません。

## 検証範囲の限界

- 画面fixtureは本物の各SwiftUI画面をホストし、ネットワークを使わないダミー番組・画像なし・仮の保存ファイルを使います。動画デコード、実RootTabの全ライフサイクル、実際のアラート操作や指での連続タップを証明するものではありません。
- 描画成功や44ptの操作領域検査は、全ての視覚・操作上の問題がないことを意味しません。生成画像の視覚レビューは別に行います。
- 日付は実行時からの相対値です。OSをまたぐピクセル完全一致のスナップショット比較ではありません。
- iOS 16の実ランタイム、実端末でのPiP・回転・VoiceOver、日本からの実配信、実際のモバイル通信での保存は別途確認が必要です。
- 地域制限、DRM、広告SDK、認証などのアクセス条件を回避する処理は追加していません。


## 2026-09-09 継続開発・第1統合

検証したコードは `63248c8d5b9969f88197c7b6b1eba7e89480b9d1`。基準は `bc1b98d74df2225a68d089eedb7986bc97a1808d` です。公開済みの長時間時計診断は再適用せず、今回は次の5範囲を限定修正しました。

- **ライブ共有**：番組情報が存在してもタイトルが空・空白の場合は、番組名・シリーズ名・局名・「リアルタイム配信」を適切に使い分けます。公式URLと既存の共有文型は維持しました。
- **ダウンロード復元**：他セッションの列挙待ちでも既知の停止意図を反映します。繰り返し復元では、未完了の自動再開待ちレコードが完全一致した場合だけ、それまでの再開意図を引き継ぎます。所有権・adoption・ネットワーク制限・再開拒否の確認は引き続き必要です。
- **見逃し配信の期限**：取得済みの結果も再生提示前に再確認し、検索を待っている間に期限が到達した結果も提示しません。期限不明の従来動作は維持しました。
- **配信期限表示**：24〜28時の放送時刻表記と全角数字を扱い、不正な日付・時刻から架空の期限を作りません。注入した暦でも実時刻を維持する構築とし、DSTの回帰テストも加えました。
- **キャッシュ再検証**：古い304応答が新しい保存内容を上書きしたり、削除したキャッシュを復活させたりしないよう、完全一致するsnapshotと時刻を確認します。これは書戻しの保証であり、呼出し元が受け取る本文のlatest-winsまで保証する変更ではありません。

### 回帰検証

独立した静的レビューの後、親のSimulatorで旧production＋新テストの失敗を確認してから修正版を検証しました。「失敗メソッド数」と「失敗アサーション数」を区別します。

| 旧productionによる対照 | 実行メソッド | 失敗メソッド | 失敗アサーション |
| --- | ---: | ---: | ---: |
| 共有文字列 | 11 | 2 | 8 |
| 復元ライフサイクル | 49 | 3 | 13 |
| 見逃し配信の期限 | 26 | 3 | 11 |
| 期限文字列解析 | 14 | 7 | 27 |
| キャッシュ書戻し | 17 | 3 | 12 |

修正済み復元49件、DownloadCenter 13件、CellularResume 15件、共有11件も選択実行で成功しました。最後に全体を一度実行し、**659件・1件スキップ・失敗0件、xcodebuild exit 0** を確認しました。環境はXcode 26.6／iOS 26.5／iPhone 17e Simulatorです。唯一のスキップは明示opt-inが必要な実TVerライブ再生です。基準のテストを削除せず23メソッドを追加し、既存のXCTAssert行を削除していないことも確認しました。新規のexpected-failureマスクは使用していません。

これはローカル実行の件数です。公開後のmain CIの成功・件数・成果物内容は別の証拠で確認します。

### 画像確認と限界

同じ全体実行で従来の画面fixtureを19枚生成しました。そのうち次の2枚を、元PNGのSHA256を照合した後、原寸320×812の8-bit sRGB閲覧コピーで確認しました。

- [番組表・最大文字サイズ](ui-review/2026-09-09-a/guide-320-accessibility5-list-fallback.png)：日付操作、現在ボタン、局名、折り返された時刻、先頭の番組名の配置を確認。
- [見逃し一覧・最大文字サイズ](ui-review/2026-09-09-a/schedule-list-320-accessibility5.png)：検索欄、日別件数、複数行の先頭番組名の配置を確認。

極大文字の固定viewportなので、下方の本文は画面外へ続きます。実スクロール操作、全行、残り17枚を目視承認したという意味ではありません。長時間時計のテストも全体実行に含まれますが、その診断画像は今回再保存していません。今回の5修正はproductionの画面レイアウト変更を含みません。

再生制御、地域切替、購読情報、通知・診断ログ、バッジの別候補はこの第1統合には含めていません。実機PiP、VoiceOver、実バックグラウンド転送、日本の実放送、iOS 16の実ランタイムは引き続き未検証です。

## 2026-09-09 継続開発・第2統合

基準は第1統合の公開版 `137c5f0d5133bfe5cd2b56d6edd9d16b8411b8c7`。最終的に全体テストを実行したコードは `a073e494b83a0551c367f76df68082257b15066b` です。画像監査版 `f5031a24ebebe8df6e506e5971d90913a5cd1f2c` は、この検証版と同じapp・test・script・project・workflow内容を持ち、追加差分は監査用の6 PNGだけです。

### 限定した修正

- **再生意図とシーク競合**：解決待ちで明示した再開・再一時停止の意図を扱い、旧requestの遅延したシーク完了が、新requestのシークを確認済みにしたり追加シークを開始したりしないようにしました。
- **購読の新着表示**：接続復帰による保留項目の再試行と新着確認を分離し、再試行だけで新着確認の完了・時刻・結果を更新しません。表示は「前回の新着確認結果」と明示します。
- **地域切替**：待機中の地域要求にも所有IDと地域を対応させ、同じ地域への読込みは実行中・待機中の要求に合流します。置き換えられた待機要求は取得を開始しません。
- **通知一覧**：遅い予約一覧の取得結果が、キャンセル済みの予約を画面へ復活させないようにしました。
- **診断ログ**：Authorization・Cookie・機密metadataキーの除去と再処理の一貫性を改善。既存保存ログも読込み時に再サニタイズし、表示・export・再保存へ渡します。サニタイズ後にキーが衝突しても辞書生成でクラッシュせず、曖昧な値を採用しません。即時の安全消去、全I/O失敗、クラッシュ時までの永続化保証ではありません。
- **バッジ**：ラベルの文字色だけを明暗に応じたsemantic label色に変更。種類を表すアイコン・背景・輪郭の色とレイアウトは維持しました。

静止したスクラブ保持については、既存の保持機構をproductionのホスト上で検証する回帰テストを追加しました。この項目は新しいproduction修正ではありません。第2統合のcode/test差分は17ファイルで、既存のテストメソッドを削除せず23メソッドを追加し、既存XCTAssert行の削除も0件です。

### 対照・反復・全体検証を区別する

1. **旧productionの対照**：基準137版へ新テストを投影した実行は、xcresultで98メソッド・77成功・21失敗・スキップ0。コンソールの失敗アサーションは82件でした。このうち静止スクラブの1メソッド・12アサーションはfixtureの準備不足であり、productionの不具合検出数に含めません。残る20メソッド・70アサーションと分離します。初期のコンソール抽出96件ではなく、正式なxcresultとsuiteの98件を採用しました。キー衝突のクラッシュ候補はこの対照から除外し、後の修正版で実行しています。
2. **選択実行**：先行統合 `12a36f93556ab117bf5e4dd9b788e69761e72048` では10 suite・116メソッドが成功、スキップ・失敗とも0。地域の既存suiteなど範囲が異なるため、対照98件と同一scopeの増減として比較しません。静止スクラブの新メソッドはこの段階には含まれません。
3. **fixture修正と反復**：通常のplay/pause経路を通り、1秒のWAV・item observer・ready・有限duration・seek可能・停止状態を確認するfixtureへ修正。修正版の静止スクラブ1メソッドを、processを再起動する明示的な3反復で3回とも成功しました。xcresultの集約はユニークな1メソッドで、実行3回とは別です。
4. **意図的なmutation対照**：保持開始時の所有フラグ代入だけを一時的に取り除くと、fixture準備が成立した状態で1メソッド・3アサーションが意図どおり失敗しました。テストは変更せず、productionファイルを原本bytesへ完全に戻しました。このmutationをcommit・公開していません。
5. **全体実行**：復元後の `16d156b3ebe17327d5a1b58a021d811c507e1a4f` で682件・1スキップ・失敗0、従来19画像を生成。その後、下方のバッジを見るための撮影条件を追加した最終 `a073e494b83a0551c367f76df68082257b15066b` でも、フィルタなしの全体を一度実行し、**682件・681成功・1スキップ・失敗0・expected failures 0、xcodebuild exit 0** を確認しました。最終runのAll testsは2026-09-09 10:32:51 UTCに終了しています。

環境はXcode 26.6／iOS 26.5／iPhone 17e Simulator。両全体実行の唯一のスキップは、明示opt-inが必要な実TVerライブ再生です。UI撮影は有効にし、only/skip filter、自動retry helper、失敗時の自動再試行、Simulator消去は使っていません。公開後CIの結果・件数は、このローカル検証とは別に確認します。

### バッジの画像確認と追加撮影

最初の19画像から、AX3ライト／ダークとAX5ライトの初期位置3枚を個別確認しました。しかしバッジはviewportの下方にあり、この3枚だけで文字色を承認できませんでした。これらの元画像も監査用に残しています。

既存19撮影とPlayer bounds・44pt・時計間隔・cleanupの検査を保持したまま、同じnative Listを最下部へ位置決めする3条件を追加しました。元のcount=19検査も残し、最終count=22を追加。位置決めは3回までとし、実offsetの前進と再計算したbottomへの到達を検査します。独立した静的レビューと、上述の最終全体テストを通過しました。画像の存在や位置決めログだけを成功の根拠にはしていません。

追加3 PNGの原本SHA256を照合し、リサイズ・切抜きなしの8-bit sRGB閲覧コピーを1枚ずつ確認しました。

- [AX3ライト・スクロール後](ui-review/2026-09-09-b/shared-row-presence-320-accessibility-scrolled.png)：黒い「見逃し配信あり」「まもなく終了」の全文が、それぞれの淡色ピル内に収まり、アイコンと区別して読めます。
- [AX3ダーク・スクロール後](ui-review/2026-09-09-b/shared-row-presence-320-accessibility-dark-scrolled.png)：白い同2ラベルが暗い色付きピル内に収まり、互いに重なりません。
- [AX5ライト・スクロール後](ui-review/2026-09-09-b/shared-row-presence-320-accessibility5-scrolled.png)：同2ラベルが2行へ折り返され、文字を縮小せずピル内に収まり、下の説明とも分離しています。

これはcatchUp／expiringSoonのバッジ部分についての視認です。別の自動pixel検査では7種類×ライト／ダークの通常文字サイズを検査していますが、7種類全ての日本語・極大文字を目視承認したという意味ではありません。元19枚を含む全22枚の視認や、物理的な指スクロール・VoiceOverの検証にも置き換えられません。

**別途切り分ける観測**：AX5のスクロール後画像では、上部のinlineナビゲーション見出しがスクロール本文に重なって写っています。バッジの文字色変更とは別に、撮影ホストの影響か実画面でも生じるかを確認する必要があります。画面全体を問題なしと承認したものではなく、この観測を画像から隠す修正はしていません。

継続候補は、自動ダウンロード受理直後の取消意図、保存予算超過時のお気に入り表示、更新されたライブ情報と公式導線のpredicate不一致、満杯の通知枠への追加失敗時の既存予約保全です。これらを第2統合で解決済みとはしていません。実機PiP・回転・VoiceOver、日本の実配信、iOS 16実ランタイムも未検証です。

## 2026-09-09 継続開発・第3統合

基準は第2統合の公開版 `e94842c87077437af1b277cb8a0e77baba135516`。3組の独立レビュー済み候補を順番に統合し、コード `621011e339cbd4dc100750251f859ec230657fbf` の全体テストを実行しました。appの変更は6ファイル、testの変更は4ファイルで、既存テストメソッドを維持して23メソッドを追加しています。共有protocol・project・script・workflowの変更はありません。

### 修正内容と境界

- **自動ダウンロードの取消・削除**：受理されたことが分かる保留番組について、後から保存状態がなくなったときは自動で作り直しません。受理の証拠を保存・復元し、API一覧から消えた後や再起動後にも引き継ぎます。実DownloadCenterの取消・完了直後の削除、失敗後の再試行拒否も検査しています。証拠のない旧保存データや、購読処理が一度も観測していない手動開始・削除から取消意図を推測するものではありません。
- **マイリストの保存予算**：履歴を省いても上限に収まらない場合、番組ID・タイトル・シリーズ情報・放送表示・配信期限などを保持し、説明・画像情報を省いたcompact metadataを保存します。それでも収まらなければ直前の保存済みデータを保持し、今回の変更が未保存であることを告知します。IDだけを残して有用な番組情報をすべて置換する動作を避けます。
- **保存状態の表示**：詳細省略・履歴省略・番組情報のない旧登録を区別して説明し、再起動後にも告知を復元します。分類件数は登録数を表示し、番組情報がない状態を「登録なし」と扱いません。compact保存にも出る告知から、誤解を招く「元に戻る」「履歴を消す」の案内を除き、閉じる操作は維持しました。
- **ライブ公式導線**：失敗表示を実際に出す場合だけ、独立した公式ボタンを省略します。同じライブIDに失敗状態が残っていても、更新された情報が配信休止・配信なしなら公式ライブページへの導線を残します。onAir/paused/unavailable・current/noncurrent・failure/nilの12条件を検査し、既存の再生開始・再試行・停止・PiP・地域切替の経路は変更しません。
- **通知予約の復元と順序**：満杯の列挙済み予約から自アプリの後発予約を退避させた後、新規追加が失敗した場合は、元のID・タイトル・本文・日時・文字列userInfoを復元します。復元にも失敗した場合は元エラー・復元エラー群・未復元ID群を保持します。別種類の通知を枠確保のために削除しません。schedule/update/cancel/cancelAllはprocess内の共通gateで順序付け、補償・same-ID cleanupまで終えてから次の変更を進めます。

通知の保証範囲は、このサービスを経由し、adapterが列挙する予約モデルです。OS全体のtransaction、別producerとのatomicity、任意の通知trigger/soundの完全復元、実配送の保証ではありません。getterはgate外のため一時状態を見る可能性があり、返らないbackendによる待機や、長時間待機中の日時経過も別の制約です。

### 対照と修正版の検証

1. **初回の修正前対照**：旧productionへの投影では80メソッド中68成功・12失敗・スキップ0、失敗アサーション39件。ライブラリ保存・購読取消・公式導線・通知復元の反例を観測しました。ライブ部分は旧動作と同値の判定helper抽出を含む対照です。投影した6ファイルは原本bytesへ復元しました。
2. **実取消・削除と拒否後の復元**：追加対照は2メソッドが失敗し、失敗アサーション14件。実DownloadCenterの取消／削除と、再試行を拒否された失敗記録の復元を検査しました。14テストの失敗ではありません。投影した2ファイルを復元しました。
3. **通知並行処理の追加対照**：1メソッドに3反復を要求しましたが、最初のsuiteは0件、後の2回でメソッドが実行され、先行add保留中に別addが入るというアサーションが失敗しました。3回とも実行したとは数えず、予約数が65件になった実証とも扱いません。投影は復元済みです。
4. **各段階の選択実行**：ライブ統合 `33d7bdb8bbc2676b3d804c946916e49177e576bf` で29件、購読・保存統合 `2d72003d0beea8feae057ef633b63768580cba4f` で94件、通知統合 `621011e339cbd4dc100750251f859ec230657fbf` で25件が成功し、それぞれ失敗・スキップ0。段階ごとの別scopeであり、最新HEADで148件をまとめて一度実行したという意味ではありません。
5. **統合版の全体実行**：`621011e339cbd4dc100750251f859ec230657fbf` をフィルタなしで実行し、**705件・704成功・1スキップ・失敗0・expected failures 0、xcodebuild exit 0**。All testsは2026-09-09 12:31:00 UTCに終了しています。Xcode 26.6／iOS 26.5／iPhone 17e Simulator、UI撮影を有効にし、22 PNGを寸法・SHA256・更新日時とともに保全しました。実TVerライブ再生のopt-inスキップは解除していません。自動retry helper・失敗時の自動再試行・Simulator消去は使っていません。

### 未確認事項を残す

今回の22 PNGは生成・保全までで、追加の目視承認はしていません。既存の大文字撮影で見えた上部ナビゲーション見出しの重なりは、撮影ホストと実画面の切り分けが未完了です。sceneが接続されていない撮影windowという観測だけで原因を断定しません。

公式ボタンの公開アクセシビリティAPIによる操作テスト案は、URL受信の因果関係・可視範囲・遅延List・same-IDの保持状態に関する独立レビュー指摘があり、この統合のテストには採用していません。別の一時的なcalibrationでは、既知のSwiftUIボタンを置いたscene接続済みkey windowでも公開APIの探索結果が0要素となりました（2メソッド中、診断1失敗・撮影opt-in 1スキップ、コンパイルは成立）。この探索方法の成立が確認できないため、実際の公式ボタン操作の合否判定には進めていません。productionのボタンが不正であるという実測ではなく、診断は原本へ復元し、commit・公開していません。判定ロジックとproduction配線の確認を、実ボタン操作・VoiceOver・指入力の成功と混同しません。保存告知の実画面dismiss、実機PiP・回転、日本の実配信、iOS 16実ランタイムも別途検証が必要です。公開後CIの結果はローカル検証とは別に確認します。
